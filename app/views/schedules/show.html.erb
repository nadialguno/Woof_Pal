
<div class="justify-content-center container py-5">
  <div class="row">
    <div class="col-3 text-center">
      <div class="sticky-top">
        <%= cl_image_tag @schedule.dog.photo.key, class: 'img-fluid rounded-circle', height: 200, width: 200, crop: :fill %>
        <p class="mb-0"><%= @schedule.dog.age_display %> old</p>
        <h1 class="mb-0"><%= @schedule.dog.name.capitalize%><small class="h4">'s</small></h1>
        <p>Schedule</p>
         <div class="margin-top: auto">
       <%=link_to "Sync", download_dog_schedule_path(@schedule.dog), class: "btn btn-primary" %>
      </div>
      </div>
    </div>
    <div class="col-9">
      <div class="list-group">
        <% @schedule.next_appointments.each do |appointment| %>
          <%= render "schedules/appointment", appointment: appointment %>
        <% end %>
      </div>
    </div>

  </div>
  <div class="text-end">
    <%= link_to "Vets near me", services_path(type: "veterinaria"), class: "btn btn-secondary me-2" %>
    <%= link_to "Groomers near me", services_path(type: "estetica canina"), class: "btn btn-secondary" %>
  </div>
</div>
